# Integration tests for optional Tier-1 store adapters (Docker services).
name: Adapters

on:
  push:
    paths:
      - "adapters-py/**"
      - ".github/workflows/adapters-ci.yml"
  pull_request:
    paths:
      - "adapters-py/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  adapters:
    runs-on: ubuntu-latest
    services:
      qdrant:
        image: qdrant/qdrant:v1.17.0
        ports:
          - 6333:6333
      neo4j:
        image: neo4j:5
        env:
          NEO4J_AUTH: neo4j/testpass
        ports:
          - 7687:7687
          - 7474:7474
      elasticsearch:
        image: docker.elastic.co/elasticsearch/elasticsearch:8.15.3
        env:
          discovery.type: single-node
          xpack.security.enabled: "false"
          ES_JAVA_OPTS: -Xms512m -Xmx512m
        ports:
          - 9200:9200
        options: >-
          --health-cmd "curl -s http://localhost:9200 >/dev/null || exit 1"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 30

    env:
      ASTROCYTE_QDRANT_URL: http://localhost:6333
      ASTROCYTE_NEO4J_URI: bolt://localhost:7687
      ASTROCYTE_NEO4J_USER: neo4j
      ASTROCYTE_NEO4J_PASSWORD: testpass
      ASTROCYTE_ELASTICSEARCH_URL: http://localhost:9200

    steps:
      - uses: actions/checkout@v6

      - uses: astral-sh/setup-uv@v8.0.0
        with:
          version: "latest"

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Wait for Neo4j Bolt
        run: |
          for _ in $(seq 1 45); do
            if timeout 1 bash -c 'cat < /dev/null > /dev/tcp/127.0.0.1/7687' 2>/dev/null; then
              echo "neo4j bolt up"
              exit 0
            fi
            sleep 2
          done
          echo "neo4j bolt not reachable"
          exit 1

      - name: astrocyte-qdrant
        working-directory: adapters-py/astrocyte-qdrant
        run: |
          uv sync --extra dev
          uv run pytest tests -v --override-ini="addopts="

      - name: astrocyte-neo4j
        working-directory: adapters-py/astrocyte-neo4j
        run: |
          uv sync --extra dev
          uv run pytest tests -v --override-ini="addopts="

      - name: astrocyte-elasticsearch
        working-directory: adapters-py/astrocyte-elasticsearch
        run: |
          uv sync --extra dev
          uv run pytest tests -v --override-ini="addopts="
