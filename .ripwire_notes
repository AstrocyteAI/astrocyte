# ripwire field notes v1 — one per line: <canonical-id or path> <TAB> <ISO-date> <TAB> <text> [<TAB> <HEAD sha> <TAB> <branch>]. Kept SORTED (merge-friendly union); dates are git committer-clock, not wall time; the trailing sha/branch pair is present only on provenance-stamped notes.
astrocyte-services-py/astrocyte-aml-py/aml_selfeval/retrieve.py	2026-09-08	--limit N takes the FIRST N items, and longmemeval_s_cleaned.json is ORDERED BY QUESTION TYPE — the first 30 are all single-session-user, the first 90 span only 2 of 6 types. Always pass a pre-stratified --source; an unstratified run is not comparable to any all-types figure.	4c8998600adcfd56ca856a202c5649cdc4a514c6	main
astrocyte-services-py/astrocyte-aml-py/aml_selfeval/retrieve.py	2026-09-08	Path.open() is a SYNCHRONOUS context manager and must not sit in an 'async with'. This exact defect shipped in AML's own published pipelines and was reproduced here — it raised TypeError before writing a single record, so the harness had never run end to end.	3e8512e131e993cd3df98ab40db16f7ff59eb7b0	main
astrocyte/_astrocyte.py	2026-09-08	Astrocyte.from_config() does NOT build a pipeline — _pipeline stays None and the first retain() raises ConfigError. Services must wire one (astrocyte_aml/wiring.py or the gateway's). Tests that inject a brain via create_app(brain=...) will NOT catch this.	3e8512e131e993cd3df98ab40db16f7ff59eb7b0	main
astrocyte/okf.py	2026-09-08	stale_after derives from metadata['_created_at'], NOT the typed VectorItem.retained_at, because run_lifecycle() keys off the former. Using retained_at would publish an expiry date we would never actually delete on.	3e8512e131e993cd3df98ab40db16f7ff59eb7b0	main
astrocyte/pipeline/observation.py	2026-09-08	consolidate() is fire-and-forget — /add returns 200 even when this LLM call fails. A provider timeout here degrades memory SILENTLY rather than erroring, so watch provider-side timeout counts, not HTTP status, when judging ingest health.	4c8998600adcfd56ca856a202c5649cdc4a514c6	main
astrocyte/providers/claude_cli.py	2026-09-08	embed() raises NotImplementedError. Any config using claude_cli for ingest needs a separate embedding_provider (composed via astrocyte/wiring.py).	3e8512e131e993cd3df98ab40db16f7ff59eb7b0	main
astrocyte/providers/local_embeddings.py	2026-09-08	pin device=cpu for concurrent use. sentence-transformers on Apple MPS hard-crashes the process under concurrent embed() with a Metal assertion ('A command encoder is already encoding to this command buffer').	3e8512e131e993cd3df98ab40db16f7ff59eb7b0	main
astrocyte/providers/ollama.py	2026-09-08	never run client concurrency > 1 against a default Ollama daemon. It serves -np 1, so a second request QUEUES while its client-side read timeout counts down — that produced a ~50% item-failure rate that looked like random flakiness. Concurrency also buys zero throughput here (measured flat 1->4).	4c8998600adcfd56ca856a202c5649cdc4a514c6	main
astrocyte/providers/openai.py	2026-09-08	a reasoning model given too small a max_tokens spends the whole budget thinking and returns EMPTY content — no error, no exception. Any benchmark or probe must assert non-empty responses or it will silently time empty completions and report confident nonsense. This produced a wrong throughput figure and a wrong retracted claim in one session.	4c8998600adcfd56ca856a202c5649cdc4a514c6	main
astrocyte/wiring.py	2026-09-08	the single provider resolver. Do NOT add a second one in a service package — the gateway and AML adapter each had their own and only one honoured embedding_provider, so one astrocyte.yaml meant different things per service. tests/test_wiring_agnostic.py guards it, but MUST be run in each service venv or it silently skips.	3e8512e131e993cd3df98ab40db16f7ff59eb7b0	main
