apiVersion: v1
kind: Service
metadata:
  name: {{ include "astrocyte-gateway.fullname" . }}
  labels:
    {{- include "astrocyte-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "astrocyte-gateway.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
